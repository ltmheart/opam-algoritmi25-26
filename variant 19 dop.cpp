#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
const int r = 3;
float f11(int x);
float f12 (int x);
float minEl(float[a][r]);
float dobNeg(float[a][r]);
float zapovA (float [a][r]);
float vyvidA(float a[r][r]);
int main(){
	float a[r][r];
	zapovA(a);
	vyvid
}
dVel =dVel*a[k][n];
 cout << "Dobutok vidyemnyh elementiv =" <<< dvel << endl;
cout << "Minelement = " << minEl << endl;
return 0;
float f11(int x) {
float f12 (pow(x,3)/2.1)+2.5;
return f;
}

float f12(int x) {
float f = sin(x*x)*cos(pow(x,3))-3.4;
return f;
}
float minEl float a[r] [r]
float min=a[0][0];
for(int k=0; k<r; k++){
	for(int n=0; n<r;n++){
		if(a[k][n]<0)
		dVel = dVel*a[k][n];
	}
}
return dVel;
}
void vyvidA(float a[r][r]){
	cout << "Masyv A:" << endl;
}
